{$SITE_ADDRESS} {
    encode zstd gzip

    handle /api/pong/* {
        reverse_proxy pong:8000
    }

    handle {
        root * /srv
        try_files {path} /index.html
        file_server
    }
}